# NOCTALIA VISUAL LAYER - OVERLAY MAESTRO
# Generado automáticamente el: sáb 21 feb 2026 09:44:05 CET

# [SISTEMA: COLORES]
source = /home/hypruser/.config/hypr/noctalia/noctalia-colors.conf

bezier = linear, 0, 0, 1, 1
# ----------------------------------------------------

# [MÓDULO: ANIMACIONES]
# @Title: Elástico
# @Icon: arrows-diagonal
# @Color: #6366f1
# @Tag: FLEX
# @Desc: Física de banda elástica. Las ventanas llegan, se estiran y rebotan verticalmente.


# -----------------------------------------------------
# ▄▀█ █▄░█ █ █▀▄▀█ ▄▀█ ▀█▀ █ █▀█ █▄░█
# █▀█ █░▀█ █ █░▀░█ █▀█ ░█░ █ █▄█ █░▀█
#

# -----------------------------------------------------
# Optimizado por NVL: Se ha inyectado física real de rebote
# (Overshoot del 15%) para justificar el nombre "Elástico".
# -----------------------------------------------------

animations {
    enabled = yes

    # --- BEZIERS ELÁSTICOS ---

    # "Rubber" (La Goma):
    # Sube muy rápido y se pasa bastante (1.15) para crear vibración visual.
    bezier = rubber, 0.05, 0.9, 0.1, 1.15

    # "Snap" (El Chasquido):
    # Para cerrar ventanas. Una curva inversa que empieza lenta y acelera muchísimo al final.
    # Como soltar una goma tensa.
    bezier = snap, 0.1, 1, 0, 1

    # --- VENTANAS (Bungee Jumping) ---

    # ENTRADA: 'slidevert' + 'rubber'.
    # La ventana entra desde abajo/arriba y rebota al llegar a su posición.
    # Velocidad 5: Perfecta para ver el efecto.
    animation = windowsIn, 1, 5, rubber, slidevert

    # SALIDA: 'slidevert' + 'snap'.
    # La ventana sale disparada hacia el borde vertical.
    # Velocidad 4: Rápida y seca.
    animation = windowsOut, 1, 4, snap, slidevert

    # MOVIMIENTO: Aquí suavizamos un poco (overshot menor) para no marear.
    animation = windowsMove, 1, 5, rubber, slide

    # --- FADES (Rápidos) ---
    # La elasticidad implica energía cinética, no lentitud.
    animation = fade, 1, 4, default

    # Capas: Los menús también rebotan verticalmente.
    animation = layers, 1, 4, rubber, slidevert

    # --- ESPACIOS DE TRABAJO (Trampolín) ---
    # Desplazamiento vertical con mucho rebote.
    animation = workspaces, 1, 6, rubber, slidevert
}

# [MÓDULO: BORDES]
# @Title: the_joker
# @Icon: mood-suprised
# @Color: #39ff14
# @Tag: THEME
# @Desc: Estética Joker: Verde ácido y morado profundo con resplandor eléctrico.

# -----------------------------------------------------
# Optimizado por NVL
# -----------------------------------------------------

general {
    border_size = 2

    # --- El Caos del Joker ---
    # Combinamos el Verde Neón (39ff14) con un Morado Eléctrico (9d00ff).
    # El color oscuro en medio (1a0026) es vital para que las luces parezcan separadas.
    col.active_border = rgba(39ff14ff) rgba(1a0026ff) rgba(9d00ffff) 45deg

    # Inactivo: Morado muy oscuro y sutil
    col.inactive_border = rgba(1a002655)
}

decoration {
    # --- Resplandor de Gotham ---
    shadow {
        enabled = true
        range = 18              # Un aura un poco más amplia para el efecto Joker
        render_power = 3

        # Usamos el morado intenso para la sombra.
        # Al girar el verde sobre este fondo morado, se crea un efecto óptico vibrante.
        color = rgba(9d00ff55)

        offset = 0 0
    }
}

animations {
    enabled = yes

    # Curva de flujo constante
    bezier = nv_joker_flow, 0.4, 0, 0.2, 1

    # Velocidad 30: Un giro constante que mantiene el dinamismo
    animation = borderangle, 1, 30, nv_joker_flow, loop
}

# [MÓDULO: SHADERS]

# [MÓDULO: GEOMETRÍA]

